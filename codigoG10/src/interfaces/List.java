package interfaces;
// no se puede instanciar
// no tiene atributos
// no tiene constructor
public interface List {
    int maxSize = 100;

    void add(int a);
    void remove(int a);
    int get(int a);
}
